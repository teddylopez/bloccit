<br>
<div class="ted-jumbotron">
  <% if !current_user.present? %>
  <h1 class="title">Welcome to teddit</h1>
  <div class="home-tron">
    <h3>teddit is a site for sharing links with your friends! Sign up to write about your favorite topics with the rest of the community.</h3>
    <div>
      <h4>
      <%= link_to "Sign In", new_session_path %> or
      <%= link_to "Sign Up", new_user_path, class: 'btn btn-primary' %> today!
    </h4>
    </div>
  </div>
  <% end %>
  <% if current_user.present? %>
  <h1 class="title">Welcome back, <%= current_user.name %></h1>
  <div class="home-tron clearfix">
    <div class="recents">
    <h3>Here are your most recent posts:</h3>
      <% if(!current_user.posts.any?) %>
        <p><%= current_user.name %> has not posted anything yet.</p>
      <% else %>
        <%= render current_user.posts.limit(5).order("created_at desc") %>
      <% end %>
    </div>
    <div class="recents topics">
      <h3>Newest Topics:</h3>
      <% @topics.each do |topic| %>
        <%= link_to topic.name, topic %> <small>created <%= time_ago_in_words(topic.created_at) %> ago</small><br>
      <% end %>
    </div>
  </div>
  <% end %>
</div>

<div class="row">
  <div class="col-md-4">
    <div class="ted-col">
      <h2>Express Yourself</h2>
      <i class="fa fa-pencil fa-5x" aria-hidden="true"></i>
      <h4>Create as many topics, posts, and comments as you'd like.</h4>
    </div>
  </div>
  <div class="col-md-4">
    <div class="ted-col">
      <h2>Cast Your Vote</h2>
      <i class="fa fa-thumbs-o-up fa-5x" aria-hidden="true"></i>
      <h4>Popular posts rise to the top.</h4>
    </div>
  </div>
  <div class="col-md-4">
    <div class="ted-col">
      <h2>Mark Your Favorites</h2>
      <i class="fa fa-star-o fa-5x" aria-hidden="true"></i>
      <h4>Keep track of all of your favorite posts.</h4>
    </div>
  </div>
</div>
